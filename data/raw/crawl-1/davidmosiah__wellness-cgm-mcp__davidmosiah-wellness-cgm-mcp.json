{
  "serverId": "davidmosiah__wellness-cgm-mcp__davidmosiah-wellness-cgm-mcp",
  "status": "BOOTED",
  "tools": [
    {
      "name": "cgm_agent_manifest",
      "description": "Returns the wellness-cgm-mcp agent manifest: tool list, supported clients, env vars, recommended first calls, capabilities, privacy posture, and community links.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "client": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "cursor",
              "windsurf",
              "hermes",
              "openclaw",
              "generic"
            ]
          },
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_capabilities",
      "description": "Lists supported providers, configured providers (via env vars), available metrics, and privacy modes.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_connection_status",
      "description": "Reports the active CGM provider (dexcom | libre), why it was selected, whether credentials are present, and whether the connector will return mock vs live data. For Dexcom it includes the env (sandbox/production); for FreeStyle Libre it includes the LibreLink Up region.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_privacy_audit",
      "description": "Returns what wellness-cgm-mcp stores locally, what is sent to Dexcom, what is never logged, and agent rules.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_data_inventory",
      "description": "Returns the metric catalog plus thresholds (TIR ranges, GMI formula reference).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_glucose_now",
      "description": "Returns the most recent EGV (estimated glucose value) plus trend arrow if available.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_glucose_window",
      "description": "Returns all EGVs over the last N hours (default 24). `hours` is the window REQUESTED; `hours_covered` and `observed_window` state what the provider actually returned. FreeStyle Libre (LibreLink Up) caps a live read at ~12h regardless of the request, so any wider ask comes back short and `notes` says so — read the covered window, not the requested one.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "hours": {
            "type": "integer",
            "minimum": 1,
            "maximum": 72
          },
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_hypo_events",
      "description": "v0.3.3 — Detect hypoglycemia events between `from` and `to` ISO dates. Returns an array of contiguous below-threshold runs lasting ≥ `min_duration_minutes`, each with `started_at`, `ended_at`, `duration_minutes`, `min_glucose_mg_dl`, `mean_glucose_mg_dl`, `severity` (level_1 = <70 ADA Level 1, level_2 = <54 ADA Level 2), and `recovery_time_minutes` (time to first reading ≥ threshold+10). Also returns `total_events`, `total_minutes_below`, `mean_min_glucose`, `events_per_day`, a `summary` string, and `recommendations` grounded in what was actually observed. `window` is the span REQUESTED; `hours_covered`, `observed_window` and `window_truncated_by_provider` state what the provider actually returned — FreeStyle Libre (LibreLink Up) caps a live read at ~12h, so a 3-day question can be answered from half a day and `notes` says so. \"No hypos\" is only true for `hours_covered`. **MEDICAL DISCLAIMER: NOT medical advice. Do not use for treatment decisions. Hypo events should be discussed with your clinician.**",
      "inputSchema": {
        "type": "object",
        "properties": {
          "from": {
            "type": "string",
            "description": "ISO-8601 timestamp / date of the analysis window start."
          },
          "to": {
            "type": "string",
            "description": "ISO-8601 timestamp / date of the analysis window end."
          },
          "threshold_mg_dl": {
            "description": "Hypo threshold in mg/dL. Default 70 (ADA Level 1).",
            "type": "number",
            "minimum": 40,
            "maximum": 100
          },
          "severe_threshold_mg_dl": {
            "description": "Severe hypo threshold in mg/dL. Default 54 (ADA Level 2).",
            "type": "number",
            "minimum": 40,
            "maximum": 80
          },
          "min_duration_minutes": {
            "description": "Minimum contiguous-minutes-below-threshold to count as an event. Default 15.",
            "type": "integer",
            "minimum": 1,
            "maximum": 120
          },
          "response_format": {
            "description": "Output shape. \"structured\" (default) returns the full event array. \"summary\" omits the per-event array, keeping totals + summary + recommendations.",
            "type": "string",
            "enum": [
              "structured",
              "summary"
            ]
          },
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "required": [
          "from",
          "to"
        ],
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_daily_summary",
      "description": "Returns daily glucose stats: mean, median, min/max, stdev, GMI (estimated A1C), CV, time-in-range (diabetic 70-180 + metabolic-health 70-140). `window_hours` is what was REQUESTED; every stat is computed over `hours_covered` / `observed_window`, which can be shorter — FreeStyle Libre (LibreLink Up) caps a live read at ~12h, so a 72h request yields a half-day metric and `notes` flags it. Never report these numbers as covering more than `hours_covered`.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "hours": {
            "description": "Window size; default 24.",
            "type": "integer",
            "minimum": 1,
            "maximum": 72
          },
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_time_in_range",
      "description": "Compute Time in Range (TIR), Time Below Range, and Time Above Range over a specific time window with a customizable target range. Use this for mealtime TIR (e.g. 7am-10am breakfast window), overnight TIR (e.g. 23:00-07:00), or specific date-range comparisons. Returns total_readings, readings_in_window, mean_glucose, median_glucose, and GMI (Glucose Management Indicator, estimated A1C per ADA / Bergenstal 2018: GMI% = 3.31 + 0.02392 × mean_mg_dL). Supports a `time_window` preset (\"wake\" = 06:00-22:00, \"sleep\" = 22:00-06:00, \"all\") OR explicit `start_hour` / `end_hour` (0-24, UTC) for recurring hour-of-day filtering. Defaults: 24h load, ADA 70-180 mg/dL, time_window=all. Pulls from cgm_glucose_window data; falls back to mock in unauth mode.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "start_time": {
            "description": "ISO-8601 timestamp of window start. Defaults to the earliest reading available.",
            "type": "string"
          },
          "end_time": {
            "description": "ISO-8601 timestamp of window end. Defaults to the latest reading available.",
            "type": "string"
          },
          "target_low": {
            "description": "Low end of target range in mg/dL. Default 70 (ADA).",
            "type": "number",
            "minimum": 40,
            "maximum": 200
          },
          "target_high": {
            "description": "High end of target range in mg/dL. Default 180 (ADA).",
            "type": "number",
            "minimum": 80,
            "maximum": 400
          },
          "hours": {
            "description": "How many hours of data to load before filtering. Default 24.",
            "type": "integer",
            "minimum": 1,
            "maximum": 72
          },
          "time_window": {
            "description": "Hour-of-day preset. \"wake\" = 06:00-22:00, \"sleep\" = 22:00-06:00 (wraps midnight), \"all\" = no hour filter. Default \"all\". Overridden by explicit start_hour/end_hour.",
            "type": "string",
            "enum": [
              "all",
              "wake",
              "sleep"
            ]
          },
          "start_hour": {
            "description": "Explicit recurring hour-of-day start (0-24, UTC). Use with end_hour to override time_window preset.",
            "type": "number",
            "minimum": 0,
            "maximum": 24
          },
          "end_hour": {
            "description": "Explicit recurring hour-of-day end (0-24, UTC). May be < start_hour to wrap midnight (e.g. 22→6).",
            "type": "number",
            "minimum": 0,
            "maximum": 24
          },
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_meal_response",
      "description": "Compute glucose response to a meal: baseline → peak → return-to-baseline. Returns peak delta, peak time (min after meal), and a band (excellent/good/moderate/poor).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "meal_time": {
            "type": "string",
            "description": "ISO-8601 timestamp of when the meal was eaten (e.g. '2026-05-10T13:15:00Z')."
          },
          "window_hours": {
            "description": "Hours of CGM data to load before+after; default 4.",
            "type": "integer",
            "minimum": 2,
            "maximum": 6
          },
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "required": [
          "meal_time"
        ],
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_authorize_url",
      "description": "Read-only OAuth URL generation; does not exchange tokens. Not a state mutation of glucose data. Gated by: user must open URL themselves (explicit user action). Builds the Dexcom OAuth authorize URL. The user opens it, grants access, and Dexcom redirects to your registered DEXCOM_REDIRECT_URI with an auth code. If credentials are missing, returns a hint with the exact env vars needed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "state": {
            "type": "string"
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_quickstart",
      "description": "Returns a personalized 3-step walkthrough for getting wellness-cgm-mcp from mock mode → live mode (Dexcom). Call this first when the user asks 'how do I connect my CGM?'",
      "inputSchema": {
        "type": "object",
        "properties": {
          "client": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "cursor",
              "windsurf",
              "hermes",
              "openclaw",
              "generic"
            ]
          },
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_profile_get",
      "description": "Returns the shared Delx Wellness profile (~/.delx-wellness/profile.json). Read-only. Surfaces diabetes status / non-DM context so wellness-cgm-mcp can pick the right time-in-range profile (70-180 ADA vs 70-140 metabolic-health).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_profile_update",
      "description": "Persist a partial patch to the shared Delx Wellness profile. Requires explicit_user_intent: true. Rejects any field containing oauth/token/secret/password/cookie/refresh/api_key/session — the profile is for non-secret wellness context only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "patch": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {},
            "description": "Partial WellnessProfileDocument patch. Top-level keys: profile, goals, devices, training, nutrition, preferences, safety, notes."
          },
          "explicit_user_intent": {
            "description": "Must be true. Pass only after the user explicitly asked to save/update profile data.",
            "type": "boolean"
          }
        },
        "required": [
          "patch"
        ],
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_onboarding",
      "description": "Returns the 11-question onboarding flow for the shared Delx Wellness profile. Read-only. The agent should ask these questions next so wellness-cgm-mcp (and the rest of the wellness stack) can personalize responses — non-secret data only, stored at ~/.delx-wellness/profile.json.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "locale": {
            "description": "Onboarding locale. Defaults to en.",
            "type": "string",
            "enum": [
              "en",
              "pt-BR"
            ]
          },
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_demo",
      "description": "Returns realistic example payloads of cgm_glucose_now, cgm_daily_summary, and cgm_meal_response. Use this to help users see what the connector will return before configuring Dexcom.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_libre_status",
      "description": "Reports FreeStyle Libre (LibreLink Up) configuration: region, whether LIBRELINKUP_EMAIL/PASSWORD (or a token) are set, whether a patient id is pinned, and whether reads will be live or mock. Use this to confirm the Libre path is wired before calling glucose tools with CGM_PROVIDER=libre.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cgm_libre_login",
      "description": "Authenticate against LibreLink Up using LIBRELINKUP_EMAIL / LIBRELINKUP_PASSWORD and list the sensors (connections) this account follows. Confirms the FreeStyle Libre path works end-to-end before reading glucose. Never returns the auth token. When credentials are missing it returns mock=true with a synthetic connection so the surface can be demoed without an Abbott account.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "privacy_mode": {
            "description": "Optional privacy mode: summary | structured | raw. summary omits device/patient identifiers when present; structured/raw return full payload.",
            "type": "string",
            "enum": [
              "summary",
              "structured",
              "raw"
            ]
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    }
  ],
  "stderrTail": "",
  "durationMs": 21242,
  "transport": "stdio"
}