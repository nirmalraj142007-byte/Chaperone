{
  "serverId": "ni-c__calibreweb-mcp__ni-c-calibreweb-mcp",
  "status": "BOOTED",
  "tools": [
    {
      "name": "search_books",
      "description": "Searches the library by title, author, series, publisher and tags. Calibre-Web returns every match in a single response, so broad queries on a large library are truncated client-side — totalFound reports the real match count. Book entries include per-format download URLs and a cover URL; fetch the cover image itself with get_cover.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500,
            "description": "Search term"
          },
          "limit": {
            "description": "Maximum number of books to return (default 50, max 200)",
            "type": "integer",
            "minimum": 1,
            "maximum": 200
          }
        },
        "required": [
          "query"
        ],
        "$schema": "https://json-schema.org/draft/2020-12/schema"
      }
    },
    {
      "name": "list_books",
      "description": "Lists books from one of the Calibre-Web views: new (recently added, default), hot (most downloaded), rated (best rated), discover (random selection), read / unread (per-user reading state), or all (the full library ordered by title, optionally narrowed to titles starting with a letter). Page size is a server-side setting; pass the returned pagination.nextOffset as offset to fetch the next page. The discover view is random and not paginated.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "view": {
            "description": "Which view to list (default: new)",
            "type": "string",
            "enum": [
              "new",
              "hot",
              "rated",
              "discover",
              "read",
              "unread",
              "all"
            ]
          },
          "letter": {
            "description": "Only with view \"all\": a single initial letter or digit, or \"00\" for every title (default: \"00\")",
            "type": "string",
            "maxLength": 2
          },
          "offset": {
            "description": "Pagination offset; use pagination.nextOffset from the previous call",
            "type": "integer",
            "minimum": 0,
            "maximum": 2147483647
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema"
      }
    },
    {
      "name": "list_shelves",
      "description": "Lists the shelves visible to the configured user: every public shelf plus the user’s own private ones. Use the returned id with get_shelf_books. isPublic is only reported on English-locale instances (Calibre-Web marks public shelves with a localized title suffix).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "offset": {
            "description": "Pagination offset; use pagination.nextOffset from the previous call",
            "type": "integer",
            "minimum": 0,
            "maximum": 2147483647
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema"
      }
    },
    {
      "name": "get_shelf_books",
      "description": "Lists the books on a shelf, in the shelf’s own order. Shelf ids come from list_shelves. Book entries include per-format download URLs and a cover URL; fetch the cover image itself with get_cover.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "shelf_id": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 2147483647,
            "description": "Numeric shelf id from list_shelves"
          },
          "offset": {
            "description": "Pagination offset; use pagination.nextOffset from the previous call",
            "type": "integer",
            "minimum": 0,
            "maximum": 2147483647
          }
        },
        "required": [
          "shelf_id"
        ],
        "$schema": "https://json-schema.org/draft/2020-12/schema"
      }
    },
    {
      "name": "get_cover",
      "description": "Fetches the cover of a book and returns it as an image. Book ids come from the other tools (books with id null have no cover). Calibre-Web serves the full-size cover; images over 1 MB are refused to protect the context window — use the coverUrl from the book entry instead. The reported mimeType is read from the image data, not from the response header: Calibre names every cover file cover.jpg whatever the image really is. Anything that is not a JPEG, PNG, GIF or WebP is refused rather than passed on as an image.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "book_id": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 2147483647,
            "description": "Numeric book id from search_books, list_books or get_shelf_books"
          }
        },
        "required": [
          "book_id"
        ],
        "$schema": "https://json-schema.org/draft/2020-12/schema"
      }
    },
    {
      "name": "get_stats",
      "description": "Returns the total number of books, authors, categories (tags) and series in the library.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "https://json-schema.org/draft/2020-12/schema"
      }
    }
  ],
  "stderrTail": "calibreweb-mcp: missing required environment variable(s): CALIBRE_WEB_URL\nRequired: CALIBRE_WEB_URL (e.g. https://books.example.com), CALIBRE_WEB_USERNAME, CALIBRE_WEB_PASSWORD\nThe credentials are the normal web login of a Calibre-Web user; the OPDS feed authenticates with HTTP Basic auth. If the instance allows anonymous browsing, leave BOTH username and password unset.\nOptional: CALIBRE_WEB_INSECURE_TLS=true to accept self-signed certificates\ncalibreweb-mcp: connected without configuration — tools are listed but every call will fail\n",
  "durationMs": 9271,
  "transport": "stdio"
}