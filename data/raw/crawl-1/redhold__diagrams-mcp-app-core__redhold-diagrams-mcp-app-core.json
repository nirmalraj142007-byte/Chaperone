{
  "serverId": "redhold__diagrams-mcp-app-core__redhold-diagrams-mcp-app-core",
  "status": "BOOTED",
  "tools": [
    {
      "name": "generate_diagram",
      "description": "Create a new cloud architecture diagram from a natural-language prompt. Returns the diagram id, its draw.io XML, Well-Architected warnings, score, and credits used. Costs credits.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "description": "What to draw, e.g. 'AWS 3-tier web app with ALB, EC2 Auto Scaling and RDS Multi-AZ'"
          },
          "cloud_provider": {
            "type": "string",
            "description": "aws | azure | gcp | kubernetes | oci | general (default: general)"
          },
          "diagram_type": {
            "type": "string",
            "description": "architecture | flowchart | sequence | data_pipeline | ... (default: architecture)"
          },
          "opinionated": {
            "type": "boolean",
            "description": "Apply best-practice hardening suggestions during generation (paid plans only)."
          }
        },
        "required": [
          "prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "edit_diagram",
      "description": "Apply a natural-language change to an existing diagram (e.g. 'add a Redis cache'). Creates a new version and returns the updated XML. Costs credits. Confirm with the user before calling — it mutates the diagram.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The id returned by generate_diagram / list_diagrams"
          },
          "edit_prompt": {
            "type": "string",
            "minLength": 3,
            "description": "The change to make, in plain language"
          }
        },
        "required": [
          "diagram_id",
          "edit_prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "fix_warning",
      "description": "Resolve a single Well-Architected warning (from get_warnings), leaving the rest of the diagram untouched. Creates a new version. Costs credits.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          },
          "message": {
            "type": "string",
            "minLength": 3,
            "description": "The warning's `message` (as returned by get_warnings)"
          },
          "component": {
            "type": "string",
            "description": "The warning's `component`, if any"
          },
          "warning_type": {
            "type": "string",
            "description": "The warning's `type`, e.g. no_encryption_transit"
          }
        },
        "required": [
          "diagram_id",
          "message"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "relayout_diagram",
      "description": "Automatically re-arrange a diagram's layout for readability (async). Starts the job and waits for it to finish, returning the re-laid XML + fresh warnings/score. Every re-layout costs credits based on the tokens it uses (like edit/fix) and requires confirm=true. If the job is still running when the wait elapses, returns a job_id you can poll with get_relayout_status.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          },
          "confirm": {
            "type": "boolean",
            "description": "Consent to the token-based charge (required to start)."
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "import_diagram",
      "description": "Import an existing draw.io (mxGraphModel/mxfile) XML document as a new diagram in your account. Validated and sanitized. Free (no AI).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "xml": {
            "type": "string",
            "minLength": 1,
            "description": "draw.io mxGraphModel/mxfile XML"
          },
          "title": {
            "type": "string",
            "description": "Optional title (derived if omitted)"
          },
          "cloud_provider": {
            "type": "string",
            "description": "aws | azure | gcp | ... (default: general)"
          },
          "diagram_type": {
            "type": "string",
            "description": "architecture | flowchart | ... (default: architecture)"
          }
        },
        "required": [
          "xml"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "update_diagram",
      "description": "Update a diagram's metadata or XML: rename it, change visibility (public/private — paid plans for private), or replace its XML. Pass only the fields you want to change. Free (no AI).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          },
          "title": {
            "type": "string",
            "maxLength": 200,
            "description": "New title"
          },
          "is_public": {
            "type": "boolean",
            "description": "true = public in the gallery, false = private (paid)"
          },
          "xml": {
            "type": "string",
            "description": "Replace the diagram XML (validated + sanitized)"
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "delete_diagram",
      "description": "Delete (soft-delete) a diagram you own. It stops appearing in list_diagrams and can no longer be fetched. Confirm with the user before calling — this is destructive.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "revert_diagram",
      "description": "Revert a diagram to an earlier version (from list_versions). Pass either version_id or version_number. Free (no AI).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          },
          "version_id": {
            "type": "string",
            "description": "The version's id (from list_versions)"
          },
          "version_number": {
            "type": "integer",
            "minimum": 1,
            "description": "Or the version number, e.g. 2"
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "get_diagram",
      "description": "Fetch a diagram by id — returns its title, draw.io XML, and Well-Architected score.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "list_diagrams",
      "description": "List your diagrams (newest first, cursor-paginated). Returns id, title, cloud, type.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Page size (default 20)"
          },
          "cursor": {
            "type": "string",
            "description": "next_cursor from a previous call"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "get_warnings",
      "description": "List the Well-Architected findings for a diagram (each has type, component, message). Free.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "export_diagram",
      "description": "Export a diagram as a raw file: `drawio` (open at app.diagrams.net) or `svg`. Returns the file content directly. Exports are free on every plan. Free-plan SVG exports carry a watermark.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          },
          "format": {
            "type": "string",
            "enum": [
              "drawio",
              "svg"
            ],
            "default": "drawio",
            "description": "drawio or svg"
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "list_versions",
      "description": "List a diagram's version history (ascending; `is_current` marks the live one). Use with revert_diagram / get_version.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Page size (default 20)"
          },
          "cursor": {
            "type": "string",
            "description": "next_cursor from a previous call"
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "get_version",
      "description": "Fetch a specific version's XML + Well-Architected score (e.g. to inspect before reverting). Free.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          },
          "version_id": {
            "type": "string",
            "description": "The version id (from list_versions)"
          }
        },
        "required": [
          "diagram_id",
          "version_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "get_relayout_status",
      "description": "Check the status of an async re-layout job started by relayout_diagram. Returns pending/done/failed; when done+applied it includes the re-laid XML + fresh warnings/score. Free.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "The diagram id"
          },
          "job_id": {
            "type": "string",
            "description": "The job_id returned by relayout_diagram"
          }
        },
        "required": [
          "diagram_id",
          "job_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "search_gallery",
      "description": "Search public community diagrams and curated library templates to reuse as a starting point. Returns id, title, and source (community/library). Fork one with fork_template.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Search text (matches title/description)"
          },
          "cloud_provider": {
            "type": "string",
            "description": "Filter by provider"
          },
          "diagram_type": {
            "type": "string",
            "description": "Filter by diagram type"
          },
          "source": {
            "type": "string",
            "enum": [
              "all",
              "community",
              "library"
            ],
            "description": "all (default) · community · library"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Page size (default 20)"
          },
          "cursor": {
            "type": "string",
            "description": "next_cursor from a previous call (community feed is paginated)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "fork_template",
      "description": "Copy a PUBLIC gallery diagram or curated library template into your own account (private) so you can edit it. Returns the new diagram id.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "diagram_id": {
            "type": "string",
            "description": "A public/library diagram id from search_gallery"
          }
        },
        "required": [
          "diagram_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "enhance_prompt",
      "description": "Turn a rough idea into a detailed generation prompt. Free.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 5,
            "description": "Your rough prompt"
          },
          "cloud_provider": {
            "type": "string",
            "description": "aws | azure | gcp | kubernetes | oci | general — biases the enhanced prompt toward that provider's services"
          }
        },
        "required": [
          "prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "clarify_prompt",
      "description": "Get 1–3 clarifying questions (and a suggested diagram type) for a vague prompt, before generating. Free.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "description": "Your prompt"
          }
        },
        "required": [
          "prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "get_usage",
      "description": "Show your current plan, credits remaining, and per-action cost estimates. Free.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "get_usage_history",
      "description": "List how much credit each past task (generate/edit/fix/relayout) charged — newest first, with the diagram it touched and the surface (api/sdk/mcp) that ran it. Use this to answer 'how much did each task cost?'. Also shows a running tally of tasks performed in THIS session. Free (read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Max rows to return (default 20)."
          },
          "cursor": {
            "type": "string",
            "description": "Pagination cursor from a previous call's next_cursor."
          },
          "action": {
            "type": "string",
            "enum": [
              "generate",
              "edit",
              "fix",
              "relayout"
            ],
            "description": "Filter to one task type."
          },
          "source": {
            "type": "string",
            "enum": [
              "api",
              "sdk-python",
              "sdk-ts",
              "mcp",
              "web"
            ],
            "description": "Filter to one surface."
          },
          "diagram_id": {
            "type": "string",
            "description": "Only tasks that touched this diagram."
          },
          "since": {
            "type": "string",
            "description": "ISO-8601 lower bound (inclusive)."
          },
          "until": {
            "type": "string",
            "description": "ISO-8601 upper bound (exclusive)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "whoami",
      "description": "Show the account, plan, scopes, and live/test mode of the configured API key. Free.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "list_capabilities",
      "description": "Discover the valid diagram types, cloud providers, and export formats the API supports — so you pass valid values to generate_diagram / export_diagram. Free.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    }
  ],
  "stderrTail": "diagrams-so MCP server v1.4.6 running on stdio.\n",
  "durationMs": 18688,
  "transport": "stdio"
}