{
  "serverId": "umodeler__picoberry-mcp__umodeler-picoberry-mcp",
  "status": "BOOTED",
  "tools": [
    {
      "name": "list_models",
      "description": "List available generation engines/models and their credit cost for a category. Call this before generating instead of hardcoding engine names. Returns [{ name, label, cost, paidOnly, supportsMultiView, supportsUltraMode, ultraCost, ... }] — use `name` as the engine/model value. NOTE: the two 3D surfaces are SEPARATE catalogs — `3d` is text-to-3D, `image-to-3d` is image-to-3D. An unrecognized category silently falls back to `3d`, so a guessed value returns a plausible list that is missing image-only engines.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "category": {
            "type": "string",
            "enum": [
              "3d",
              "image-to-3d",
              "image",
              "parts-board",
              "remesh",
              "texture",
              "animate",
              "uv-unwrap"
            ],
            "default": "3d",
            "description": "`3d` lists TEXT-to-3D engines only. Engines whose vendor takes image input alone (e.g. meshy-7) have no text-to-3D row and appear ONLY under `image-to-3d` — use that before generate_3d_from_image."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "list_animation_presets",
      "description": "List animation preset ids for the `animate` tool. Presets are engine-specific (Tripo ~97 / Meshy ~675) — pick one from the SAME engine you animate with. Optionally filter with a case-insensitive substring.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "engine": {
            "type": "string",
            "enum": [
              "tripo",
              "meshy"
            ],
            "default": "tripo"
          },
          "search": {
            "type": "string",
            "description": "case-insensitive filter on the preset name/category"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "get_credits",
      "description": "Get the current PicoBerry credit balance and plan for the authenticated key.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "generate_image",
      "description": "Generate a 2D image from a text prompt (async). Returns an asset { id }; then call wait_for_asset (or poll get_asset) until taskStatus=2 and read files.image (PNG URL). Costs credits — see list_models(category='image'). Omit `model` for the default. Up to 4 reference image URLs can guide the result (best with nano-banana).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "maxLength": 5000
          },
          "model": {
            "type": "string",
            "description": "engine name from list_models(category='image')"
          },
          "aspect_ratio": {
            "type": "string",
            "description": "e.g. \"1:1\", \"16:9\", \"9:16\""
          },
          "reference_image_urls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "maxItems": 4
          }
        },
        "required": [
          "prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "generate_3d_from_text",
      "description": "Generate a game-ready 3D model (GLB) from a text prompt (async). Returns an asset { id }; call wait_for_asset (or poll get_asset) until taskStatus=2 and read files.model (GLB URL). Costs credits — see list_models(category='3d'). Omit `engine` for the default.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "maxLength": 1024,
            "description": "≤1024 chars (Tripo engine limit)"
          },
          "engine": {
            "type": "string",
            "description": "engine name from list_models(category='3d') — the text-to-3D catalog"
          },
          "polycount": {
            "type": "integer",
            "exclusiveMinimum": 0
          },
          "texture": {
            "type": "boolean",
            "default": true
          }
        },
        "required": [
          "prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "generate_3d_from_image",
      "description": "Generate a 3D model (GLB) from one image, or from 2–4 views of the same subject (multi-view → higher-fidelity geometry), async. Single: `image_url` (any public http/https image, or a prior generation's files.image) OR `image_path` (a local file, uploaded directly — no hosting needed). Multi-view: `image_urls` OR `image_paths`, ordered [front, left, back, right] (2–4 views). Not every engine takes multi-view — check `supportsMultiView` from list_models(category='image-to-3d'); unsupported engines return 400. Local files win over URLs. Then wait_for_asset and read files.model. Costs credits — see list_models(category='image-to-3d') (NOT category='3d', which is the text-to-3D catalog and omits image-only engines).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "image_url": {
            "type": "string",
            "format": "uri",
            "description": "single hosted image URL"
          },
          "image_path": {
            "type": "string",
            "description": "absolute path to a single local image file (≤20MB)"
          },
          "image_urls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "minItems": 2,
            "maxItems": 4,
            "description": "multi-view: 2–4 hosted image URLs, ordered [front, left, back, right]"
          },
          "image_paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 2,
            "maxItems": 4,
            "description": "multi-view: 2–4 local image file paths (each ≤20MB), ordered [front, left, back, right]"
          },
          "engine": {
            "type": "string",
            "description": "engine name from list_models(category='image-to-3d')"
          },
          "polycount": {
            "type": "integer",
            "exclusiveMinimum": 0
          },
          "texture": {
            "type": "boolean",
            "default": true
          },
          "ultra_mode": {
            "type": "boolean",
            "description": "higher-fidelity geometry. Only engines whose list_models entry has supportsUltraMode (currently meshy-7), and SINGLE image only — the vendor scopes it to one input image. Adds that entry's ultraCost credits. Combining it with another engine or with multi-view returns 400."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "parts_board",
      "description": "Decompose one reference image into an exploded \"parts board\" image (async) — the subject laid out as separated components on one canvas. Input: `asset_id` (an existing IMAGE asset you own), `image_url` (a public http/https image), OR `image_path` (a local file, uploaded directly). Engine/resolution/prompt are server-fixed — no params. Returns an asset { id }; call wait_for_asset (or poll get_asset) until taskStatus=2 and read files.image (the board PNG). Feed that image to generate_3d_from_image for a parts-separated mesh. Costs 80 credits — see list_models(category='parts-board'). Precedence when several are set: image_path > asset_id > image_url.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "asset_id": {
            "type": "string",
            "description": "id of an existing IMAGE asset you own to decompose"
          },
          "image_url": {
            "type": "string",
            "format": "uri",
            "description": "public http/https source image URL"
          },
          "image_path": {
            "type": "string",
            "description": "absolute path to a local image file (≤20MB), uploaded directly"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "remesh",
      "description": "Retopologize an existing 3D asset into a NEW asset (async). Costs credits — see list_models(category='remesh') (pb-remesh is cheapest). wait_for_asset → files.model.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "asset_id": {
            "type": "string"
          },
          "engine": {
            "type": "string"
          },
          "polycount": {
            "type": "integer",
            "exclusiveMinimum": 0
          }
        },
        "required": [
          "asset_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "texture",
      "description": "Re-texture an existing 3D asset (PBR) into a NEW asset (async). Describe the desired look in `prompt`. Costs credits — see list_models(category='texture'). wait_for_asset → files.model.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "asset_id": {
            "type": "string"
          },
          "prompt": {
            "type": "string"
          },
          "engine": {
            "type": "string"
          }
        },
        "required": [
          "asset_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "animate",
      "description": "Auto-rig an existing 3D character asset and apply an animation, producing a NEW asset (async). Pick `preset` from list_animation_presets for the SAME engine. Costs credits — see list_models(category='animate'). wait_for_asset → files.model.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "asset_id": {
            "type": "string"
          },
          "preset": {
            "type": "string"
          },
          "engine": {
            "type": "string"
          }
        },
        "required": [
          "asset_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "get_asset",
      "description": "Get a generation's status + result. taskStatus: 0=pending 1=processing 2=succeeded 3=failed. When succeeded, files.{model,image,thumbnail,textures} hold signed result URLs (short TTL — download promptly). On failure, read errorDetail.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "asset_id": {
            "type": "string"
          }
        },
        "required": [
          "asset_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "wait_for_asset",
      "description": "Poll an asset until it finishes (taskStatus 2 or 3) or the timeout elapses, then return the final asset. Use right after generate_* / remesh / texture / animate. 3D generations can take several minutes.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "asset_id": {
            "type": "string"
          },
          "timeout_seconds": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 1800,
            "default": 600
          },
          "poll_interval_seconds": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 60,
            "default": 3
          }
        },
        "required": [
          "asset_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "list_my_assets",
      "description": "List your generated assets (newest first, owner-scoped) for browsing/sync. Filter by category (3d/image/…) or keyword.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "category": {
            "type": "string"
          },
          "keyword": {
            "type": "string"
          },
          "page": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 100,
            "default": 20
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "download_asset",
      "description": "Export a completed 3D asset and get a short-lived signed download URL. glb = a single self-contained file. fbx/obj download as a .zip bundle (model + textures; + Unity .meta files when texture_preset='unity') — unzip before importing. For Unity use fbx (there is no built-in glb importer).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "asset_id": {
            "type": "string"
          },
          "format": {
            "type": "string",
            "enum": [
              "glb",
              "fbx",
              "obj"
            ],
            "default": "glb"
          },
          "texture_preset": {
            "type": "string",
            "enum": [
              "standard",
              "unity"
            ]
          },
          "name": {
            "type": "string"
          }
        },
        "required": [
          "asset_id"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    }
  ],
  "stderrTail": "[picoberry-mcp] connected · base=https://api.picoberry.ai\n",
  "durationMs": 16440,
  "transport": "stdio"
}