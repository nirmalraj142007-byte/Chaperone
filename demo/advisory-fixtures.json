{
  "$fixture": true,
  "notice": "FIXTURE, NOT MODEL OUTPUT. Every summary and score in this file was written by a person on the date below. No model produced any of it. It exists only so the offline demo's consent card can show an advisory line instantly, with no network call. Every surface that displays one of these rows labels it as a fixture. See demo/OFFLINE.md.",
  "todo": "TODO(blocker: Bedrock calls are unavailable because of an AWS account restriction, support case open; resolves once live Bedrock calls work, before the Oct 22, 2026 submission cutoff): run `pnpm advisory:run-local` for the demo mutation, record what the model really says, and either replace these rows with that output (modelId set to the real model, no longer a fixture) or delete this file and let the card show its advisory-unavailable state.",
  "writtenOn": "2026-09-23",
  "modelId": "fixture:hand-written-not-model-output",
  "rows": [
    {
      "id": "grocery-add-item-calendar-clause",
      "describes": "demo-upstream's scripted mutation of add_item (POST /control/mutate). The row is attached to the hash of the definition the upstream really serves after that call, so it can only ever appear next to that exact change.",
      "upstreamId": "grocery",
      "toolName": "add_item",
      "mustContainAddedText": "check the household calendar for the next 7 days",
      "score": 72,
      "summary": "The new wording tells the assistant to look through your calendar for the next 7 days whenever it adds something to the shopping list. The version you approved never mentioned a calendar, and a shopping list does not need one."
    }
  ]
}
